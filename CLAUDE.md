# Claude Code 구현 계약

## 역할과 기준 문서

GPT는 WHY / WHAT / 게임 규칙 / UX / 우선순위 / 수용 기준과 리뷰를 담당한다. Claude Code는 HOW / 기술 설계 / 구현 / 리팩터링 / 테스트 / 빌드를 담당한다.

작업 전에 README, GAME_DESIGN, CORE_LOOP, SYSTEM_SPEC, DECISIONS, 해당 WP를 읽는다. 채팅 기억 대신 저장소의 확정 문서를 기준으로 삼는다. 문서가 충돌하면 DECISIONS의 최신 확정 기록을 확인하고 충돌을 명시한다. 구현 편의를 위해 게임 목표나 수용 기준을 몰래 바꾸지 않는다.

## 작업 단위

- 한 번에 하나의 WP를 수행한다. 권장 브랜치명은 `wp/001-enemy-flow` 형식이다.
- 상태: DRAFT → READY → IN_PROGRESS → REVIEW → DONE. 막히면 BLOCKED와 이유를 기록한다.
- DRAFT는 선행 WP 리뷰 후 기준을 다듬어 READY로 전환한다.
- READY인 WP의 일상적인 기술 선택은 조사 후 근거를 기록하고 진행한다. 게임 규칙·범위 변경은 제안으로 남겨 GPT 검토를 받는다.
- 선행 WP가 DONE이 되기 전에 후속 기능을 섞지 않는다.
- 현재 구현 요청은 문서 초기화와 별개다. 저장소 생성만으로 WP가 완료된 것이 아니다.

## 구현 원칙

1. 유도 → 압축 → 시설 섬멸이라는 경험을 유지한다.
2. 경로, 전투, 시설, 네트워크, 웨이브 상태의 책임을 분리한다. 구체적 클래스나 ECS 사용은 구현자가 선택한다.
3. 대량 적의 생성·이동·타기팅 비용을 측정한다. 최적화 방식은 측정 결과에 근거한다.
4. 밸런스 수치는 설정 데이터로 분리하고 테스트 시드·시나리오를 기록한다.
5. 현재 WP의 Non-Goals는 구현하지 않는다. 기존 파일과 에셋을 보존한다.
6. 비밀키, 토큰, 로컬 사용자 경로, 제삼자 에셋을 커밋하지 않는다.

## 검증과 완료 출력

- 각 AC에 대응하는 자동 검증 또는 재현 절차와 관측 결과를 남긴다.
- 실제 플레이 화면으로 경로 변화, 병목, 사격, 후퇴 등 해당 WP의 경험을 확인한다.
- 성능 결과에는 엔진/빌드, OS, CPU/GPU/RAM, 해상도, 적 수, 측정 구간, 평균 FPS, p95 프레임 시간과 메모리를 기록한다.
- 미실행 테스트는 NOT RUN, 실패는 FAIL로 기록한다. 수치나 스크린샷을 만들었다고 주장하지 않는다.
- `results/RESULT_TEMPLATE.md`를 `results/WP-XXX-RESULT.md`로 복사한다. 재리뷰는 이전 결과를 보존하며 날짜별로 추가한다.
- 기준 커밋과 구현 커밋, 변경 파일, 실행 방법, AC별 증거, 알려진 문제, 설계 변경 제안, 다음 WP 영향을 기록한다.
- 코드 커밋 후 결과 문서에 그 커밋을 기록하고 별도 문서 커밋을 만들어 자기참조 해시 문제를 피한다.
- 검증 완료 후 REVIEW로 전환한다. GPT의 PASS와 실패/미실행 기준 해소 후 DONE으로 전환한다.

## 리뷰 계약

GPT는 실제 diff와 증거를 함께 검토한다. PASS는 모든 필수 AC 충족, 범위 준수, 재현 가능한 결과가 있을 때만 가능하다. REVISE에는 파일/행 또는 재현 시나리오, 기대 동작, 필요한 수정·재시험을 기록한다.

API 기반 자동 실행, CI, 배포, 새 패키지 계정 연결은 현재 초기 구조에 포함되지 않는다.

## 실행 환경 (WP-001 이후)

- 엔진: Godot 4.7.stable, GDScript, 2D 탑다운 (DECISIONS D-007~D-009). 설치·실행·테스트·빌드·성능 측정 명령은 README "실행 환경" 절과 `scripts/verify.*`에 있다.
- 시뮬레이션 규칙은 `game/core/`의 SceneTree 비의존 클래스에만 둔다. `game/scenes/`는 상태를 읽고 명령만 전달한다. 헤드리스 테스트(`tests/`)가 `game/core`를 직접 구동하므로 이 경계를 유지한다.
- 밸런스·시드·시나리오 값은 `game/core/config.gd`에 두고 `--set key=value`로 덮어쓴다. 테스트는 시드를 명시한다.
- 회귀 확인은 `godot --headless --path . --script res://tests/run_tests.gd` 종료 코드 0을 기준으로 한다. 결과 문서의 증거는 `results/evidence/`에 둔다 (캡처 PNG, 테스트 리포트, 성능 JSON).


## 게임 품질 평가 지침 (2026-09-21)

기획·코어루프·시나리오·밸런스·게임샷·UI/연출 리뷰는 [GAME_EVALUATION_GUIDE](docs/GAME_EVALUATION_GUIDE.md)를 함께 따른다. 기능 AC 검증과 플레이 경험 평가를 구분하고, 문제마다 관찰 근거·플레이어 영향·개선안·재검증 방법을 기록한다. 지침은 기존 AC를 소급 변경하거나 비범위 기능을 자동 승인하지 않는다.
