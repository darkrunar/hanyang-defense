# System Specification

상태: WP-001/002 구현 검증 완료, WP-003 READY. 규칙·수치는 각 WP와 [DECISIONS](DECISIONS.md)의 최신 확정 기록을 따른다. 세부 구현 자료구조는 Claude Code가 선택한다.

## 시스템 책임

| 시스템 | 입력 | 출력·책임 | 첫 WP |
|---|---|---|---|
| EnemySpawner | 시드, 생성 수, 진입점 | 적 생성과 생존 수 기록 | 001 |
| PathNetwork | 진입점, 목표, 통행 상태 | 유효 경로와 경로 버전 | 001 |
| Barricade / Placement | 배치·제거 요청 | 통행 변경 또는 거절 이유 | 001 |
| DensityDetector | 생존 적 위치, 반경 | 후보 영역별 적 수 | 001 |
| Hwacha | 후보 영역, 사거리, 재사용 시간 | 표적 결정, 광역 피해 | 001 |
| BongsuNetwork | 시설, 연결 거리, 활성 상태 | 연결 그룹과 공유 표적 정보 | 002 |
| District / DefenseLine | 거점 상태, 침입 피해 | 붕괴, 후퇴, 목표 갱신 | 003 |
| WaveDirector | 웨이브 설정, 생존 적, 거점 상태 | 다음 웨이브 또는 런 종료 | 003 |
| RunGrowth | 자원, 선택 이벤트 | 시설·도시 기능 변화 | 후속 |

## WP-001: 흐름과 화력

- 테스트 맵은 세 진입로와 공통 방어 목표를 갖는다. 경로에 식별자를 부여한다.
- 적은 유효한 경로로 이동한다. 배치·제거가 경로 버전을 바꾸면 영향을 받는 적이 갱신된 통행 상태를 반영한다.
- 초기 정책: 어느 진입점이든 목표까지 가는 모든 길을 막는 배치는 거절한다. 실패 시 통행 상태와 배치 상태 모두 원상 유지한다.
- 이미 적이 점유한 위치에는 장승을 놓을 수 없다. 파괴·제거 후 경로가 다시 열려야 한다.
- 시설은 2×2 셀을 점유하며 다른 시설과 겹칠 수 없다. 통행 차단은 장승만 한다. 화차의 점유는 시설 중복 배치를 막을 뿐 적의 통행을 막지 않으며, 적은 화차 footprint를 그대로 통과한다 (DECISIONS D-011, GPT WP-001 한정 승인. 봉수대 등 다른 시설의 통행 규칙은 별도 결정).
- 점유 판정은 적의 현재 좌표를 기준으로 한다. 이동 스텝이 끝난 시점의 위치와 배치 검증이 보는 위치가 항상 같아야 하며, 시각 오버레이의 가능/불가능 표시도 같은 판정을 쓴다.
- 밀도는 설정된 원형 반경 안의 생존 적 수로 정의한다. 경계 위의 적도 포함한다.
- 화차는 사거리 안의 후보 영역 중 적 수가 가장 많은 영역을 겨냥한다. 동률이면 안정된 후보 ID 순서로 선택한다. 적이 없으면 발사하지 않는다.
- 피해는 범위 안의 생존 적에 한 번만 적용하고, 사망 처리는 한 번만 집계한다.
- 생성 누계와 동시 생존 수를 구분한다. 1,000개체 테스트는 처치를 끈 구간에서 동시 생존 수를 먼저 입증한다.

## WP-002: 연결에 따른 행동 변화 (DONE, 2026-09-14)

확정 상세 계약·초기값·고정 fixture·AC-01~08은 [WP-002](../backlog/WP-002.md), 기획 결정은 DECISIONS D-012~015를 따른다. 이 절의 규칙은 초기 제안을 구체화한 구현 기준이다.

- 활성 봉수대만 중계한다. 중심 거리 180px 이하의 무방향 그래프를 구성하며 센서/화차는 범위 내 최근접 봉수대 하나에만 부착한다(동률 시설 ID). 단말 간 직접 연결·중계는 없다.
- 센서 탐지 140px, 화차 로컬 탐지 100px, 사거리 200px. 탐지는 현재 적 좌표, 사거리는 후보 영역 중심 기준이며 경계를 포함한다. 시야 차폐·통신 지연은 없다.
- 각 화차는 로컬 적 ID와 같은 그룹 센서의 적 ID 합집합으로 후보 영역 밀도를 계산한다. 중복 센서·순환망·로컬 중복을 제거한다. 부분 관측에서 전역 밀도를 노출하지 않는다. 최고 밀도/영역 ID 동률 규칙과 광역 피해는 WP-001을 유지한다.
- 시설 변경과 이동을 처리한 틱의 사격 전에 연결·탐지를 갱신한다. 단절·사망·센서 소멸/범위 이탈 후 마지막 탐지 출처가 없으면 즉시 폐기한다. 다른 출처가 남으면 유지한다. 연결 변화로 쿨다운을 초기화하지 않는다.
- 봉수대·센서는 2×2 점유지만 적 통행을 막지 않는다. 기존 현재 적 점유·중복·지형·경계 배치 검증을 사용한다. 비활성은 점유를 유지하고 기능만 중단, 제거는 점유도 해제한다. 시설 HP/공격 시스템은 범위 밖이다.
- WP-001 검증 모드는 기존 규칙을 보존하고 WP-002 모드는 로컬 탐지 제한을 적용한다. 새 모드의 행동 차이를 회귀 실패로 혼동하지 않으며 모드를 결과에 명시한다.
- 기본 16시설(화차4/봉수대8/센서4)의 고정 배치에서 네트워크 이동/전투를 각각 측정한다. 전투는 연결 전환12회와 장승 경로 변경12회, 실제 공유 사격을 포함한다. D-009 예산과 매 프레임 1000체 유지·원시 프레임/생존 배열 검산을 필수로 한다.

## WP-003: 검증과 후퇴 (DONE, 2026-09-15 GPT 재리뷰 PASS `f5d7e69`)

2026-09-15: [WP-003 READY v1.0](../backlog/WP-003.md)을 구현 계약으로 확정했다. 정확한 fixture·웨이브·입력 시각·AC·합격선은 해당 WP를 따른다. READY는 구현 착수 승인이고 실제 AC는 구현 후 검증한다.

- 외곽 목표 (47,26), 핵심 (47,10), 내곽 x42~53/y6~22. 초기18시설(기존 fixture B 16+외곽 장승2), 전용10존(Z8/Z9 추가). 기존 WP-001/002 데이터·도달 순서 보존.
- 외곽HP360/핵심HP60/도달피해1 (D-032 후속 보완 기준; 기존 제출 구현HP120은 REVISE). WP-003은 이동→사격→생존 도달 처리. 같은 틱 외곽 초과 피해는 핵심에 전달하지 않는다.
- 외곽HP0에서 붕괴1회. 생존 적 ID·HP·좌표를 보존하고 핵심 경로로 전환한다. 외곽 시설 기능은 중단하되 점유·장승 차단은 유지한다.
- 중영 H1만 동일 ID로 회수. 분리 중 쿨다운 동결, 일반 비활성 화차는 기존 감소 정책 유지. 내곽의 현재 합법 배치 성공1회에만 권리 소비.
- 전투를 계속하며 핵심HP 버퍼로 조작 기회를 제공한다. 하드5초 제한은 없고 F2/F3에서 붕괴+5초 생존·유효 배치를 검증한다.
- 유한3웨이브 총1140체. 모든 생성 종료+적0+핵심생존이면 승리, 핵심0 패배 우선. 종료 후 정지와 run_id를 통한 재시작/지연 명령 무효화를 적용한다.
- F1 정상 승리, F2 실제 웨이브 강제 붕괴 후 승리, F3 H4를 비활성한 통제 A/B 비교, F4 종료·초기화. F3는 B 공유사격≥1·처치 개선≥6·핵심 피해 감소≥6을 요구한다.
- 성능만1000체 즉시 보충·핵심 보호. move/combat 각각 측정20초 붕괴·25초 B 재배치, 실제 사건·원시 프레임·생존 수·출처를 검증한다. 정상 승패 증거에는 보호 설정을 사용하지 않는다.

기획 결정 D-019~027이 적용된다. 구현상 일반 기술 선택은 Claude가 기록하고 진행하며 규칙·밸런스·합격선 변경은 제안으로 남긴다.

## 데이터와 관측

적, 시설, 경로, 구역, 웨이브는 안정된 ID를 갖는다. 위치·HP·활성 상태·목표 ID와 시드, 경로 버전, 연결 그룹을 관측할 수 있어야 한다. 디버그 오버레이에는 생존 수, 경로, 밀도, 선택 표적, 연결 상태, 누수 수를 필요한 WP에서만 표시한다.

## 성능 검증 계약

D-009에서 채택한 예산은 1080p 독립 릴리스 빌드, 동시 생존 1,000개체, 준비 10초 후 60초 측정, 평균 60 FPS 이상과 p95 프레임 시간 25ms 이하이다. WP-002에도 적용하며 고정 시설·이벤트 부하는 해당 WP를 따른다. 매 프레임 alive≥1000을 원시 생존 배열로 증명하고 FPS·p95를 원시 프레임 배열로 검산한다.

생성·이동만의 성능과 화차·피해·경로 변경이 활성화된 성능을 각각 기록한다. 충돌·막힘·메모리 증가도 함께 확인한다. 측정 환경이 없으면 NOT RUN이며 성능 기준은 통과하지 않은 것이다. 5,000개체는 이후 확장 조사다.

## WP-004: 플레이 흐름·메뉴·재시작 (DONE, 2026-09-20 GPT 재리뷰 PASS `d9a445c`)

[WP-004](../backlog/WP-004.md)와 D-039~042가 세부 계약이다. 일반 실행은 TITLE에서 시작하고 PLAYING/PAUSED/SETTINGS/CONFIRM/RESULT를 RunState와 분리한다. 메뉴를 여는 것은 전투 틱과 배치를 멈추며, RESULT는 실제 종료 상태를 따른다.

진행 중 R/재시작·시작 화면 복귀는 확인을 요구하고 취소하면 진입 상태를 보존한다. 결과 후 재시작은 확인 없이 같은 시드/초기 상태로1회 시작한다. UI 이벤트는 전장으로 전달하지 않고 held/pending 입력·이전 run_id·선택을 초기화한다. 저장은 창 모드 설정만 대상으로 하며 런 중간 저장은 제외한다.

결과 모델은 종료 장부에서 시간·웨이브·처치·외곽/핵심 도달·붕괴·재배치/위치·핵심HP를 확정한다. 기존 perf/capture/headless 모드는 메뉴/저장 설정을 우회한다. 기존 모드의 실제 존·시설과 전투/성능 계약을 유지한다. 구체적인 메뉴 구현 자료구조는 Claude가 선택한다.
