# Decisions

기준일: 2026-09-13. 출처는 사용자 요청과 ‘디펜스 게임 기획’ 대화(6aa64e85-21e8-83ee-ac0b-8f9883a0b664)다. 원본 대화 전체를 저장소에 복제하지 않는다.

## 확정

| ID | 결정 | 근거·영향 |
|---|---|---|
| D-001 | 한양 전체를 무기화하는 대규모 전투 디펜스 | 사용자가 목표를 명시적으로 확정 |
| D-002 | 조선 사이버펑크, 로그라이트 디펜스 | 현재 사용자 요청의 장르·콘셉트 |
| D-003 | GPT 기획·리뷰, Claude Code 구현·테스트 | Git의 명세·WP·결과를 전달 기준으로 사용 |
| D-004 | ~~비공개 GitHub 저장소~~ (대체됨 → D-010) | 초기화 시점의 기본 공개 범위. 2026-09-13 사용자 요청으로 공개 전환 |
| D-005 | 검증·붕괴·후퇴·재편을 코어 루프에 포함 | 도시 방어체계의 약점 대응을 중심으로 유지 |
| D-006 | 초기 캐릭터 방향에 포졸·사또 포함 | 기존 사용자의 추가 요청; 세부 능력·인원은 미정 |

## 초기 구성 제안

| ID | 제안 | 상태·이유 |
|---|---|---|
| P-001 | 저장소 식별자 hanyang-defense | 저장소 초기화용 이름; 정식 게임 제목 아님 |
| P-002 | WP-001 흐름·화차 / WP-002 봉수망 / WP-003 붕괴·재편 | 이번 초기화에서 구성한 작업 분할. 이전 대화의 12개 기술 작업 예시를 세 개의 경험 검증 단위로 묶음 |
| P-003 | 첫 검증 동시 생존 적 1,000개체 | 기존 첫 프로토타입 예시를 채택; 5,000은 장기 탐색 목표 |
| P-004 | 모든 길 차단 배치 거절, 동률 표적 ID 순서 | 재현 가능한 첫 프로토타입 규칙 |
| P-005 | 연결 표적 공유, 두 구역 후퇴와 화차 1대 회수 | 최소 범위에서 연결·재편 가설 검증 |
| P-006 | 약 25~30분 런, 물자·정보·혼 경제 | 플레이테스트와 후속 명세가 필요한 기획 가설 |

P-002~005는 초기 WP의 구현 기준으로 제공한다. 바꾸려면 영향과 이유를 기록하고 기획 검토를 거친다. 사용자가 직접 모든 세부 수치를 확정한 것으로 표현하지 않는다.

## 미결정

| ID | 항목 | 처리 시점 |
|---|---|---|
| O-001 | ~~엔진·버전, 2D/3D·시점, 대상 플랫폼~~ | 해소 → D-007, D-008 (2026-09-13) |
| O-002 | ~~기준 장비, 빌드 설정, 성능 합격 예산~~ | 해소 → D-009 (2026-09-13). SYSTEM_SPEC 잠정안을 그대로 채택 |
| O-003 | 정식 제목, 세계관 고유명사 | 별도 기획 |
| O-004 | 최종 조작 체계, 캐릭터 능력·로스터 | 현장 개입 WP |
| O-005 | 자원 경제, 로그라이트 선택·메타 성장 | 핵심 가설 검증 이후 |
| O-006 | 아트 제작 방식, 에셋 권리, 라이선스 | 실제 에셋 도입 전 |

## 새 결정 기록 양식

```text
ID / 날짜 / 상태(제안·채택·대체됨):
문제와 선택:
근거와 고려한 대안:
영향받는 명세·WP:
결정 주체와 검증 증거:
대체하는 이전 결정:
```

기존 결정은 삭제하지 말고 새 ID로 대체 관계를 남긴다. 기술 선택은 Claude가 근거와 함께 기록하며, 게임 범위·규칙 변경은 GPT가 검토한다.

## WP-001 기술 착수 결정 (2026-09-13, Claude Code)

아래는 CLAUDE.md의 "READY인 WP의 일상적인 기술 선택은 조사 후 근거를 기록하고 진행한다" 규칙에 따라 Claude Code가 조사·채택한 결정이다. 게임 규칙·범위에는 영향이 없으며, 규칙 변경이 필요한 사항은 별도로 "제안"에 둔다.

```text
D-007 / 2026-09-13 / 채택:
문제와 선택: 엔진·버전 미결정(O-001). Godot 4.7.stable (official, GDScript) 채택.
근거와 고려한 대안:
  - 조사 환경: Windows 11 Home 26200, Godot 4.7.stable(PATH 등록, Windows release export template 설치됨),
    Unity 에디터 5종(2021.3.45f2, 6000.0.76f1, 6000.2.10f1, 6000.4.1f1, 6000.4.10f1), Node 22.18, Python 3.13.
  - Godot: CLI만으로 헤드리스 테스트(`godot --headless --script`), 창 실행, 1080p 독립 실행 릴리스 빌드
    (`--export-release`)까지 전부 수행 가능. MIT 라이선스로 공개 저장소에 엔진 의존 파일을 두어도 권리 문제 없음.
    2D MultiMesh로 1,000개체 렌더링이 단순하고, 시뮬레이션 코어를 SceneTree 비의존 클래스로 분리해 AC별 자동 검증이 가능.
  - Unity: 설치는 되어 있으나 UnityMCP 연결 실패(ConnectionRefused), batchmode 반복 비용이 크고 Windows 빌드 모듈·
    라이선스 상태가 미검증. 실제 실행 화면과 성능 측정을 이 세션에서 재현 가능하게 남기기 어려움.
  - Web/TypeScript: 검증은 쉬우나 장기 목표(25~30분 런, 5,000개체 탐색, 에셋·사운드)에 대한 엔진 기반이 약함.
  - 기존 대화의 Unity 언급은 README/DECISIONS에 따라 확정으로 간주하지 않음.
영향받는 명세·WP: WP-001~003 전부. 저장소에 project.godot, game/, tests/, export_presets.cfg 추가.
결정 주체와 검증 증거: Claude Code. results/WP-001-RESULT.md의 실행·테스트·빌드 기록.
대체하는 이전 결정: 없음 (O-001 해소).
```

```text
D-008 / 2026-09-13 / 채택:
문제와 선택: 화면 방식·렌더러. 2D 탑다운 회색상자, 렌더러 gl_compatibility(OpenGL 3.3), 1920x1080 논리 해상도,
  stretch canvas_items/keep, vsync 비활성(측정 기본값).
근거와 고려한 대안:
  - 2D 탑다운은 "어디에서 싸우게 만들 것인가"(경로·병목·밀도)를 가장 직접적으로 보여준다. 3D는 WP-001 범위 밖의 비용.
  - gl_compatibility는 2D 회색상자에 충분하고 드라이버 표면이 가장 작다. 측정 장비에는 디스플레이 어댑터가 3개
    (RTX 4070 SUPER, Radeon iGPU, Parsec 가상 디스플레이) 있어 Vulkan보다 위험이 낮다고 판단. 창 실행에서 RTX 4070 SUPER로
    동작 확인. forward_plus 전환은 project.godot 한 줄이며, 2D 조명·셰이더가 필요해지면 재검토한다.
  - 20px 셀 × 96×54 격자 = 1920×1080. 격자 기반 Dijkstra 흐름장(8방향, 모서리 끼기 금지)으로 경로를 계산한다.
영향받는 명세·WP: SYSTEM_SPEC의 데이터·관측 항목을 game/core에서 구현. WP-002 이후 그래픽 방향은 O-006에서 별도 결정.
결정 주체와 검증 증거: Claude Code. results/evidence/captures/*.png.
대체하는 이전 결정: 없음.
```

```text
D-009 / 2026-09-13 / 채택:
문제와 선택: 기준 장비·빌드 설정·성능 합격 예산(O-002). SYSTEM_SPEC의 잠정안을 낮추지 않고 그대로 채택:
  1080p 독립 실행 릴리스 빌드, 동시 생존 1,000개체, 준비 10초 후 60초 측정, 평균 60 FPS 이상, p95 프레임 시간 25ms 이하.
  이동 전용 시나리오와 경로 변경·전투 활성 시나리오를 각각 측정하고, 정적 메모리(시작/종료/피크)를 함께 기록한다.
근거와 고려한 대안:
  - 기준 장비: Windows 11 Home 10.0.26200 / AMD Ryzen 5 7600 (6C/12T) / NVIDIA GeForce RTX 4070 SUPER / 63.2 GB RAM.
    주의: Parsec 가상 디스플레이 어댑터가 함께 설치되어 있어 프레젠테이션 경로가 일반 환경과 다를 수 있다. 이를 배제하기 위해
    vsync를 끄고 순수 프레임 비용을 측정하며, 결과 문서에 이 사실을 명시한다.
  - 빌드: `godot --headless --export-release "Windows Desktop Release"` (embed_pck, x86_64). 측정은 `--perf` 모드가
    벽시계 프레임 간격을 기록해 JSON(results/evidence/perf/)으로 남긴다.
  - 이 장비는 목표 사양이 아니라 기준 장비다. 최소 사양 정의는 후속 결정.
영향받는 명세·WP: WP-001 AC-07. 5,000개체는 이후 확장 조사(P-003 유지).
결정 주체와 검증 증거: Claude Code. results/WP-001-RESULT.md 성능 절.
대체하는 이전 결정: 없음 (O-002 해소).
```

```text
D-010 / 2026-09-13 / 채택 (사용자 요청):
문제와 선택: 저장소 공개 범위. 비공개(D-004)에서 공개(Public)로 전환. github.com/darkrunar/hanyang-defense 는 현재 Public.
근거와 고려한 대안: 사용자가 공개 전환을 요청. 공개에 따라 인증정보·개인 경로·권리 미확인 에셋을 커밋하지 않는다는 규칙을
  .gitignore와 리뷰 절차로 유지한다. 폰트는 저장소에 포함하지 않고 Godot의 시스템 폰트 폴백(한글)에 의존한다.
영향받는 명세·WP: README의 공개 범위 문구. 모든 WP의 커밋 검토.
결정 주체와 검증 증거: 사용자 요청, Claude Code 반영. `gh repo view`로 visibility=PUBLIC 확인(2026-09-13).
대체하는 이전 결정: D-004.
```

```text
D-011 / 2026-09-13 / 채택 (구현 해석 → GPT 리뷰 0211f3d에서 WP-001 한정 승인):
문제와 선택: 화차가 통행을 차단하는지 SYSTEM_SPEC에 명시가 없다. GAME_DESIGN이 "장승(길 차단), 화차(광역 화력)"으로 역할을
  나누므로 WP-001에서는 장승만 통행을 차단하고 화차는 2x2 점유만 하며 통행을 막지 않는다. 두 시설 모두 같은 배치 검증
  (경계·지형·중복·적 점유)을 거치고, 장승만 전체 경로 차단 검사를 추가로 거친다.
근거와 고려한 대안: 화차가 차단까지 하면 화차 배치가 경로 차단 거절에 걸려 화력 배치가 경로 설계와 얽힌다. 대안(화차도 차단)은
  한 줄 변경으로 가능하며 GPT가 원하면 되돌린다.
영향받는 명세·WP: SYSTEM_SPEC WP-001 절에 명시함(2026-09-13). WP-002 봉수대의 통행 규칙은 자동 확정되지 않음.
결정 주체와 검증 증거: Claude Code. tests/test_path_and_placement.gd "hwacha placed on open ground".
대체하는 이전 결정: 없음.
```

## WP-001 구현 중 발견한 제안 (GPT 검토 대상)

| ID | 제안 | 상태·이유 |
|---|---|---|
| P-007 | 포화 골목의 장승 설치 규칙 재검토 | **GPT 판정(0211f3d): 현행 점유 거절 규칙은 WP-001에서 유지.** "적이 점유한 위치에는 장승을 놓을 수 없다"를 그대로 구현하면 1,000개체 포화 상태에서 남대문 골목 2×2 앵커의 설치 성공률이 낮다(리뷰 재실행 5.28%, 구현자 측정 3.6~11.4% — R-01 수정 전 수치이며 고정 보장치가 아님. R-01 수정 후 재측정: 스윕 65/2400 = 2.71%, 테스트 스윕 38/720 = 5.28%; **한 앵커를 누르고 있을 때 실제 대기시간 20회 시행 중 19회 성공, 최소/평균/최대 1.08 / 7.66 / 25.58초, 1회는 30초 안에 실패** — `results/evidence/occupancy_probe.json`). 후속 제안: "선택 위치에 예약 → 셀이 비는 순간 경로·중복·점유 재검증 후 확정"을 별도 명세(취소·상태 표시 포함) 후 구현. 밀어내기 방식은 적 흐름을 즉시 바꾸는 전투 능력이므로 채택하지 않음 |
| P-008 | 서·동대문의 남편 골목은 평시 미사용 | **GPT 판정(0211f3d): WP-001에서는 의도된 우회 선택지로 수용.** 관찰. 목표가 북쪽에 있어 서·동 경로의 남편 골목은 북편 골목이 막혔을 때만 쓰이는 우회로가 된다(AC-03 테스트로 검증). 남대문 두 골목은 대칭이라 자연 분산된다. 의도된 우회로로 볼지, 지형을 바꿔 평시에도 분산시킬지는 기획 판단 |
| P-009 | 밸런스 초기값 | **GPT 판정(0211f3d): 재현용 프로토타입 초기값으로 승인.** 적 HP 60 / 화차 피해 34 / 재장전 0.8s / 폭발 반경 55 / 사거리 200. "골목 통과 중 2발로 처치"가 성립하도록 맞춘 값(피해 25·재장전 1.2s에서는 골목 화차의 처치가 0). 출시 난이도나 "2발이면 항상 처치"의 보장이 아니며 재미·경제·웨이브 밸런스는 후속 검증. 모든 값은 game/core/config.gd, `--set key=value`로 변경 |

## WP-002 READY 기획 결정 (2026-09-14, GPT)

사용자의 WP-002 기준 정리·READY 전환 요청에 따라 GPT가 아래를 채택했다. 구현·성능 검증은 Claude Code의 다음 작업이며 아직 PASS를 부여하지 않았다. 상세 수치·AC·fixture의 기준 문서는 backlog/WP-002.md다.

| ID | 채택 결정 | 근거·영향 |
|---|---|---|
| D-012 | 봉수대만 중계, 180px 무방향 간선. 센서/화차는 최근접 활성 봉수대 하나에 부착(동률 ID), 단말 비중계 | 봉수대가 연결 설계의 역할을 유지하고 단말의 다중 부착으로 분리망이 합쳐지는 모호함을 제거. 그룹은 연결 요소 최소 봉수대 ID. 초기 WP-002 그래프 제안을 구체화 |
| D-013 | 센서140px·로컬100px·사거리200px. 개체 ID 합집합으로 화차별 알려진 밀도 계산, 사격 전 최신 정보 검증 | 연결이 새 사격 기회를 만들되 사거리/피해 보너스를 부여하지 않음. 부분 관측·중복·단절·사망·슬롯 재사용 규칙 명시. WP-001 전용 검증 모드 보존 |
| D-014 | 봉수대·센서도 2×2 점유/통행 비차단, 현재 적 점유 배치 거절. 비활성은 점유 유지·기능 중단 | D-011의 화차 규칙을 새 두 시설에 명시적으로 확장. P-007 예약 설치는 후속으로 유지. 시설 HP/적 공격 구현은 제외하고 디버그 비활성으로 단절 검증 |
| D-015 | 4화차·8봉수대·4센서 고정 fixture와 네트워크 이동/전투 각각 D-009 예산 적용 | WP-001 리뷰의 실제1000체 부하·원시 프레임 검산 요구 계승. 전투에서 연결12회/경로12회 변경 및 실제 공유 사격 증명. 수치 최적화·경제 검증과 구분 |

선행 작업: WP-001 DONE 및 GPT PASS d9699aa, PR #1 병합 확인. WP-002 READY는 이 기준으로 구현을 시작할 수 있다는 뜻이며 구현 완료·새 성능 보장·WP-003 READY를 뜻하지 않는다.

## WP-002 기술 결정 (2026-09-14, Claude Code)

```text
D-016 / 2026-09-14 / 채택:
문제와 선택: 봉수망 자료구조와 갱신 시점. 별도 모듈 game/core/bongsu_network.gd가 활성 봉수대 간선(중심 거리² ≤ 180², 격자 좌표라 정수로 정확)·
  연결 요소(그룹 id = 최소 봉수대 id)·단말 부착(최근접, 동률 id)을 계산하고, 매 틱 이동 후·사격 전에 센서 탐지/화차 인지 집합을 처음부터 다시 만든다.
  캐시나 TTL이 없으므로 "출처가 없는 표적은 다음 사격 전에 사라진다"가 구조적으로 보장된다. 시설 명령은 그래프를 즉시 재계산하고
  (관측 가능성), 탐지는 틱에서만 갱신한다.
근거와 고려한 대안: 이벤트 전파형(센서 갱신 시 그룹으로 push)은 순환·중복·오래된 표적 처리를 별도로 증명해야 한다. 봉수대 8·센서 4·적 1000
  규모에서 전체 재계산 비용은 센서 4×1000 + 화차 4×1000 거리 검사 수준(측정: network_combat p95 참조).
영향받는 명세·WP: SYSTEM_SPEC WP-002. WP-003의 구역 붕괴는 시설 비활성 명령으로 표현 가능.
결정 주체와 검증 증거: Claude Code. tests/test_bongsu_network.gd, results/WP-002-RESULT.md.
대체하는 이전 결정: 없음.
```

```text
D-017 / 2026-09-14 / 채택:
문제와 선택: 적 개체 식별. EnemySim에 슬롯별 세대 카운터를 두고 enemy_id = (gen << 16) | slot 로 개체를 식별한다. 공유 표적 집합은 이 id를
  키로 써서 재사용 슬롯의 새 개체가 이전 개체로 오인되지 않는다 (WP-002 계약 "풀 슬롯 재사용은 세대로 구분").
근거와 고려한 대안: 전역 증가 id 배열(메모리 동일)·객체 참조(GDScript에서 느림). 세대 방식은 기존 SoA 풀과 O(1) 검증(is_id_alive)을 유지.
영향받는 명세·WP: WP-003 이후 모든 표적/피해 기록.
결정 주체와 검증 증거: Claude Code. AC-03 slot reuse 테스트.
대체하는 이전 결정: 없음.
```

```text
D-018 / 2026-09-14 / 채택:
문제와 선택: WP-001 검증 모드 보존. config targeting_mode("wp001" 전역 밀도 / "wp002" 로컬+공유)와 fixture("wp001" 화차 4 / "b" 16시설 / "none")를
  두고, WP-001 테스트·캡처·성능 시나리오는 Config.for_wp001()로 고정한다. 기본 플레이는 wp002 + fixture b.
근거와 고려한 대안: 규칙 분기를 화차 코드 한 곳(network==null 이면 전역 카운트)으로 한정해 회귀 범위를 좁힘. WP-001 회귀 135건이 동일 결과.
영향받는 명세·WP: README 실행 방법, results/WP-002-RESULT.md 모드 명시.
결정 주체와 검증 증거: Claude Code. results/evidence/wp-002/tests/test_report.txt (WP-001 절 포함).
대체하는 이전 결정: 없음.
```

## WP-002 구현 중 발견한 제안 (GPT 검토 대상)

| ID | 제안 | 상태·이유 |
|---|---|---|
| P-010 | fixture A "로컬 사격" 단계의 기하 | **GPT 판정(8ddab9d): 보조 화차 H2 anchor(48,41)=(980,840) 안 승인.** 정확한 원 경계 최단 거리는 sqrt(40²+150²)−45 ≈ 110.24px(구현자의 112px은 근삿값). 보조 화차는 fixture A 로컬 검증 단계에 한정하며 fixture B의 화차 4·영역 8·기본 100px 규칙은 불변, 새 영역·범위 확대 불필요. 원 제안: fixture A의 H(46,29)=(940,600)은 WP-001 후보 영역 8개 중 가장 가까운 Z0/Z1 원의 가장자리까지 112px로, 로컬 탐지 100px 안에 있으면서 후보 영역 안에 있는 적이 존재할 수 없다. 따라서 "4. 로컬 적을 별도로 넣어 단절 중 로컬 사격을 입증"은 H로는 불가능하며, 구현은 동편 골목에 보조 화차 H2(48,41)=(980,840)를 두고(Z1까지 90px 로컬, fixture 적까지 120px 비로컬) 같은 단절 상태에서 로컬 사격을 입증했다. 대안: (a) 현행(보조 화차) 수용, (b) 광장 남단 합류점 후보 영역 추가(단, fixture B "8개 밀도 영역"과 WP-001 모드 보존에 영향), (c) fixture A의 H 위치 변경. 기획 판단 요청 |
| P-011 | 벤치마크 부하 유지와 처치 수 | 관찰. network_combat의 처치·공유 사격 수는 benchmark_hold_alive 즉시 보충 하에서 얻은 값이라 일반 플레이 밸런스 자료가 아니다(P-009 판정과 동일 취지). 결과 문서에 명시 |


## WP-003 DRAFT 제안 이력 (2026-09-15, GPT; 아래 READY 결정으로 해소)

아래는 초안 당시의 제안 이력이다. **현재 P-012~016은 아래 D-022~027 및 WP-003 READY v1.0으로 채택·구체화되었다.** 기존 두 구역·붕괴1회·화차1대 무료 재배치·승패 규칙은 유지한다. 상세 초안과 READY 체크리스트는 [WP-003](../backlog/WP-003.md)을 따른다. 구현 결과 또는 확정 밸런스로 인용하지 않는다.

| ID | 제안 | READY 전 확인 |
|---|---|---|
| P-012 | 두 목표/구역 및 도달 처리: 이동→사격→생존 적 도달, 같은 틱 외곽 초과 피해는 핵심에 전달하지 않음 | 구역 경계·거점 좌표·전 진입로 접근성, 기존 즉시 도달 제거의 변경 영향, 중복 ID/동시 피해 처리 |
| P-013 | 붕괴 외곽 시설은 비활성·점유 유지, 장승의 통행 차단도 유지. 외곽 재건/재활성은 거절 | 핵심 목표 변경 후에도 경로가 존재하는지, 점유와 기능 중단의 UI 표현. 철거로 자동 개방하는 대안은 별도 판단 |
| P-014 | 지정 화차1대의 동일 ID·스탯·쿨다운을 회수 상태에 보존, 회수 중 쿨다운 동결, 내곽 유효 배치 성공 때만 권리1회 소비 | 실패 클릭·중복 요청·종료 후 명령·초기화, 무료 재장전/시설 복제 방지, 전투 중 배치 시간 확보 |
| P-015 | 기존 적·세 진입로의 유한3웨이브, 마지막 남문 물량 집중. 제안 생성 누계1140체·외곽HP120/핵심HP60/도달피해1 | 난이도 미검증. 정상 방어 승리·붕괴 후 승리·핵심 패배 재현과 웨이브 종료 조건 검증. 동시1000체 성능 부하와 구분 |
| P-016 | 동일 붕괴 스냅샷 A/B 재편 비교,1000체 전환 벤치마크는 일반 유한 웨이브와 별도 모드 | 비교 배치/관측 구간/개선 임계값을 사전 고정. 후보 영역 기하·실제 fixture·전환 사건·원시 부하·구간별 집계·빌드 출처 검증 |

WP-002는 GPT PASS 869788b 및 PR #2 병합으로 DONE이다. WP-003의 당시 DRAFT 유지 사유는 아래 READY 결정으로 해소되었다.

## WP-003 결정 요청 판단 (2026-09-15, GPT)

사용자가 제시한 D-a~D-d 요청에 대한 정책 판단이다. 이 절은 최초 판단 이력이며 당시 미판정이던 D-c/e~h는 아래 READY 결정에서 해소했다. 좌표의 계산상 가능성과 실제 게임 검증 결과를 구분한다.

| ID | 결정 | 근거·영향 |
|---|---|---|
| D-019 | WP-003 전용 Z8(950,560)/r70·Z9(950,410)/r50 추가, 기존8존 포함10존. WP-001/002의8존 보존 | D-a 채택. 외곽 목표 주변과 내곽 진입부의 타기팅 공백 보완. 알려진 적 기반 밀도·사거리 규칙 유지. 실제 F1/F3 합격은 구현 후 AC 리뷰에서 검증 |
| D-020 | 회수 대상은 초기 화차·중영(46,29)의 안정 ID | D-b 채택. 임의 화차 선택·복제 회수 없이 기존1대 회수 계약 구체화. ID 보존/쿨다운 정책 등 상세 회수 초안은 별도 READY 검토 유지 |
| D-021 | 조작 기회는 핵심HP 버퍼, 하드5초 제한 없음. F3의 양쪽 배치 시점을 붕괴+5초로 고정 | D-d 채택. 전투는 계속 진행. +5초 핵심 생존·합법 배치 가능을 요구하고 +0/5/10초 지연도 관측. HP60은 검증 전 제안이며 숨은 보호시간 금지 |

D-c의 A(52,12)/B(44,13)는 비교 후보로 수용한다. B2/B3를 포함한 내곽 전체 봉수대·센서 배치가 없어 A의 완전 비연결과 B의 센서 연결은 아직 확정하지 않는다. 제시된184.4/164.9px는 보고값으로만 보존한다. D-e~D-h는 상세 본문 수신 후 판단한다.

## WP-003 READY 확정 (2026-09-15, GPT)

사용자의 진행 요청에 따라 main `2efac7d`에 병합된 사전 검토와 후속 자료를 확인하고 기하 probe 2개를 재실행했다. 원문을 찾지 못했던 D-e~D-h는 이제 수신·판정 완료다. 아래 결정은 위 DRAFT/보류 표현을 대체하며 상세 계약은 [WP-003 READY v1.0](../backlog/WP-003.md)이다. 구현 AC는 아직 NOT RUN이다.

| ID | 채택 결정 | 근거·영향 |
|---|---|---|
| D-022 | D-c: 기존 fixture B 16시설+외곽 장승 (44,36)/(22,24), 내곽 x42~53/y6~22, 두 목표와 A(52,12)/B(44,13) 확정 | A 미부착184.39px, B→B2 164.92px, B2–B3–S4 망 확인. 장승 유지 후 세 경로 접근 가능. 추가 내곽 장승 없음. Z9 r50 유지, 센서가 전체 원을 본다고 가정하지 않고 개체별 관측 검증 |
| D-023 | P-012/013 및 D-e: WP-003 사격 후 생존 도달, 같은 틱 초과 미전달, 붕괴 한 번·장승 차단 유지. WP-001/002 즉시 도달 모드 보존 | 회귀 격리. 상태 전환으로 경로 재계산1회·생존 ID/HP/좌표 보존. 외곽 점유 유지·재활성 금지 |
| D-024 | P-014 및 D-f: 동일 H1 회수/배치, 분리 중에만 쿨다운 동결, 일반 비활성은 현행 유지 | 무료 재장전/ID 복제 방지. 현재 적 점유를 포함한 성공 배치만 권리 소비. D-021의 HP 버퍼·5초 검증 시점 유지 |
| D-025 | P-015 및 D-h: HP120/60·피해1, 유한3웨이브1140체·5초 간격을 초기 구현값으로 채택 | 기술 제약 없음. F1/F2 실제 승리 여부는 구현 후 확인하며 실패하면 수정 제안/REVISE. 출시 밸런스 보장 아님 |
| D-026 | P-016 F1~F4와 F3 관측30초: H4 비활성 통제 비교, 동일12체·배치 시각, B 공유사격≥1/처치차≥6/핵심 피해차≥6 | 다른 내곽 화차에 의한 효과 가림을 분리. F2는 일반18시설·실제 전체 웨이브·붕괴+5초 B배치 후 승리 요구. 통제 시험을 자연 웨이브 개선률로 해석하지 않음 |
| D-027 | P-016 및 D-g:18시설·10존·1000체, move/combat 각각 측정20초 실제 피해 붕괴/25초 B배치, 의미 이벤트6건 | 성능 전용 outerHP1000000→1/coreHP60 보호·즉시 보충을 공개. 후보 평가·실제 배치·사격·원시 배열·출처 검증. 보호는 정상 승패 시험에 적용 금지 |

**기획 최종 판정: READY.** 기술 검토·수용 기준 작성과 구현 후 합격 확인을 구분한다. 기준은 확정됐으므로 Claude가 구현을 시작하며, 실제 AC 미달은 숨기지 않고 결과 문서에서 FAIL/NOT RUN 및 수정 제안으로 다룬다.

## WP-003 기술 결정 (2026-09-15, Claude Code)

```text
D-028 / 2026-09-15 / 채택:
문제와 선택: WP-003 상태를 별도 모듈로 분리. RunState(런/방어 상태·거점 HP·회수권·run_id·틱+순번 이벤트 로그)와
  WaveDirector(웨이브별 진입로 예산·초당 속도·고정 틱 누산·남→서→동 순서·5초 간격·마지막 웨이브 완료 플래그)를
  Battle이 소유하고, Battle.step이 계약의 틱 순서(생성→이동→밀도/망/탐지→사격→생존 도달→붕괴→승패→보충)를 고정한다.
근거와 고려한 대안: 기존 EnemySim/Placement/BongsuNetwork를 건드리지 않고 규칙을 얹기 위해 오케스트레이션만 확장.
  WP-001/002 기본값(Config.new)은 sandbox 모드로 그대로이고 WP-003은 Config.for_wp003() 프리셋으로 격리.
영향받는 명세·WP: SYSTEM_SPEC WP-003 절. 후속 경제/캐릭터 WP는 RunState 이벤트 로그를 재사용 가능.
결정 주체와 검증 증거: Claude Code. tests/test_collapse_retreat.gd, results/WP-003-RESULT.md.
대체하는 이전 결정: 없음.
```

```text
D-029 / 2026-09-15 / 채택:
문제와 선택: 도달 순서(D-023)와 회귀 격리. EnemySim.arrival_mode = "immediate"(WP-001/002 그대로) / "after_fire"(WP-003:
  목표 반경 안의 적은 그 자리에서 대기, 화차 사격 후 collect_arrivals()가 생존 개체만 1회 소비). 도달은 누수(leaked)로
  집계하며 처치가 아니다. 한 틱의 도달은 틱 시작 목표에 귀속(_tick_target_core).
근거와 고려한 대안: 이동 중 후보 수집 후 재검증하는 방식도 가능하나, 대기 후 소비가 더 단순하고 "도달 위치에서 사격으로 죽은
  적은 피해를 주지 않는다"를 구조적으로 보장. WP-001/002 회귀 322건 결과 동일.
영향받는 명세·WP: SYSTEM_SPEC WP-003.
결정 주체와 검증 증거: Claude Code. AC-01 doorstep 대조 테스트.
대체하는 이전 결정: 없음.
```

```text
D-030 / 2026-09-15 / 채택:
문제와 선택: 회수 화차의 동일 ID·쿨다운 동결(D-024). Placement.detach(id)는 점유만 해제하고 같은 Structure 객체를
  detached 사전으로 옮긴다. structures 목록에서 빠지므로 탐지·부착·사격·쿨다운 감소가 구조적으로 멈춘다(별도 분기 불필요).
  restore(id, anchor)는 try_place와 같은 validate()(지형·중복·현재 점유·구역)를 통과할 때만 같은 객체를 재점유시키고,
  Battle.place_recovery는 성공 시에만 회수권을 소비한다. 일반 비활성(active=false) 화차는 목록에 남아 쿨다운이 계속 줄어든다
  (계약: 기존 정책 유지).
근거와 고려한 대안: remove+새 ID 생성은 계약이 금지. 목록 제외 방식은 코드 경로 하나로 "분리 중 아무것도 하지 않음"을 보장.
영향받는 명세·WP: SYSTEM_SPEC WP-003. WP-004 이후 시설 이동/회수 경제에 그대로 재사용 가능.
결정 주체와 검증 증거: Claude Code. AC-03 테스트(거절 시 권리 보존, 성공 1회, 쿨다운 0.5 동결 vs 비활성 0.0).
대체하는 이전 결정: 없음.
```

```text
D-031 / 2026-09-15 / 채택:
문제와 선택: path_version 장부의 재시작 재현성. Battle.reset()이 경로 버전을 1로 재기준화한 뒤 재계산하므로 새 런과 재시작
  런의 버전이 같다(신규 2, fixture C 장승 2개로 4). WP-001/002 증거가 인용한 절대값은 그대로 유지된다.
근거와 고려한 대안: 절대 카운터를 그대로 두면 재시작마다 +3이 되어 "같은 시드·입력의 결과가 run_id 외 동일" 계약을 깨뜨림.
영향받는 명세·WP: 성능 JSON의 path_version_expected 계산은 이미 시작값 기준.
결정 주체와 검증 증거: Claude Code. F4 재시작 결정성 테스트.
대체하는 이전 결정: 없음.
```

## WP-003 구현 중 발견 (GPT 검토 대상)

| ID | 제안 | 상태·이유 |
|---|---|---|
| P-017 | **F1 정상 방어 승리 불성립 (계약 수치)** | 계약 D-025 초기값(외곽 HP 120, 도달 피해 1, W1 60/60/60 @10/s …)과 18시설로는 외곽이 W2 중 자연 붕괴(52.8초)하고 W3에서 핵심이 무너진다(92.2초 LOST). W1만으로도 180체 중 66체 도달(HP 120→54). 원인: 골목 존 통과 1.5초에 볼리 1~2발(처치에 2발 필요), 광장 수렴 후에는 Z8 하나만 사격. 수치를 통과 목적으로 바꾸지 않았고 FAIL로 기록. 대안(기획 판단): (a) 외곽 HP 상향(예: 300+), (b) W1/W2 생성 속도 하향(예: 5/s), (c) 광장 접근 존 추가·외곽 화차 재배치, (d) F1 합격 조건을 "외곽 붕괴 후 승리"까지 허용. 흥미로운 관찰: F2(20초 강제 붕괴 후 내곽 방어)는 핵심 HP 28로 승리 — 좁은 광화문 병목이 넓은 외곽보다 방어에 유리 |
| P-018 | 테스트 스위트 종료 코드 | F1 5건 FAIL로 `run_tests.gd` 종료 코드 1. 실패를 숨기지 않기 위해 유지. `verify.*` 1단계가 여기서 멈추므로 WP-003 증거는 단계별 명령으로 생성. F1 판정이 바뀌면 자동 해소 |

## WP-003 GPT 리뷰 후속 결정 (2026-09-15)

**D-032 · 채택 · P-017(a): 외곽 초기/최대 HP120→360.** 핵심HP60·도달피해1·전체1140웨이브·18시설·10존·스탯·F1 외곽 유지 승리 조건은 유지한다. GPT가 구현 `6e9240c` 코드에서 HP만 바꾼 진단으로 전체 외곽 도달314를 확인했고, HP360에서는99.833초 WON/외곽46/핵심60/붕괴0이었다. 근거: `results/evidence/wp-003/gpt-review/2026-09-15/review_checks.json`.

D-025의 외곽HP 값만 대체한다. 보완 구현의 초기값·재시작 테스트·UI/증거를 갱신하고 F1/F2를 재실행한다. F2/성능의 시각별 검증용 HP 강제 설정은 그대로다. 원래 HP120 기준 구현은 FAIL이며 이 결정으로 소급 PASS하지 않는다. 기존 제출 diff의 최종 판정은 `results/WP-003-RESULT.md`의 GPT Review **REVISE**다.

P-018의 실패 종료1 정책은 유지한다. R-01~07 수정 및 필수 증거 확보 전 DONE/병합을 승인하지 않는다.

## WP-003 보완 회차 기술 결정 (2026-09-15, GPT 리뷰 R-01~07 반영)

```text
D-033 / 2026-09-15 / 채택:
문제와 선택: R-02 — 존 집합은 모드의 일부다. Battle.reset()이 config.zone_set으로 DensityDetector를 다시 만든다(_init 한 번이 아니라
  매 reset). 실제 scene이 프리셋을 바꾸고 reset()을 부르는 경로(--perf move 등)에서 WP-001/002 시나리오가 8존을 갖는다.
근거와 고려한 대안: "모드 확정 후 Battle 생성"은 scene의 생성 순서를 바꿔야 하고 --set 오버라이드 시점과 얽힌다. reset에서
  재생성하면 코어 시험과 scene 경로가 같은 코드로 보장된다. 새 스위트 tests/test_scene_modes.gd가 실제 _apply_run_mode()로
  8존의 id/이름/중심/반경을 WP-001 표와 대조한다(로그 문자열이 아니라 live DensityDetector).
영향받는 명세·WP: 성능 manifest.zones는 live 존 표에서 생성.
결정 주체와 검증 증거: Claude Code. test_scene_modes.gd, perf JSON manifest.
대체하는 이전 결정: 없음(D-028 보완).
```

```text
D-034 / 2026-09-15 / 채택:
문제와 선택: R-04 — 붕괴 진입점 멱등. Battle._collapse()는 collapse_count>0 / INNER_ONLY / 종료 상태에서 즉시 false를 돌려주고
  상태·권리·경로·망·이벤트를 바꾸지 않는다(RunState.collapse_calls_ignored만 증가, 이벤트 없음). 공개 별칭 collapse()로
  계약 시험·리뷰어 프로브가 직접 호출한다. 일반 플레이의 자연 경로(_process_arrivals)는 그대로 1회다.
근거와 고려한 대안: 호출자 검사에만 의존하면 새 호출자가 생길 때 깨진다. 진입점 자체가 거절하면 구조적으로 1회다.
영향받는 명세·WP: AC-01/AC-03 중복 콜백 계약 시험(대기·배치 완료·종료 3상태, full_state 동일).
결정 주체와 검증 증거: Claude Code. test_collapse_retreat.gd "R-04".
대체하는 이전 결정: 없음.
```

```text
D-035 / 2026-09-15 / 채택:
문제와 선택: R-03 — 재시작이 scene 입력 상태를 초기화한다. R/reset은 _reset_input_state()로 홀드 클릭(+발행 run_id)·일시정지·
  잔상·알림·스크립트 오버라이드를 비운다. 홀드 재시도는 발행 run_id가 현재 run_id와 같을 때만 실행되며 다르면 버린다.
근거와 고려한 대안: 코어 run_id 검사만으로는 scene의 홀드 상태가 새 런에서 재시도되는 것을 막지 못했다(리뷰어 재현).
영향받는 명세·WP: AC-06. test_scene_modes.gd가 실제 KEY_R 이벤트로 검증.
결정 주체와 검증 증거: Claude Code.
대체하는 이전 결정: 없음.
```

```text
D-036 / 2026-09-15 / 채택:
문제와 선택: R-06 — 전체 구조화 상태 Battle.full_state(). 모든 생존 적(id·slot·경로·좌표·HP·속도·오프셋·셀), 모든 시설(지도·대기,
  좌표·구역·활성·부착·그룹·쿨다운·카운터·표적 진단), 망(간선·그룹·센서별 관측 id·화차별 인지 id), 경로(목표·버전), 존 표·카운트,
  런 상태(run_id 제외), 웨이브(누산기 포함), RNG 상태, 명령 카운터. 소수 반올림 없음. F3 A/B 사전 상태 동일성과 F4 종료 불변·
  재시작 재현성은 full_state_json() 문자열 동일성으로 판정한다(기존 state_hash는 보조).
근거와 고려한 대안: 스냅샷 복원(직렬화→복원)은 EnemySim 내부 자유 목록/RNG 복원이 필요해 범위가 크다. 같은 시드·입력 재생 +
  완전 상태 비교가 계약("동일 시드 재편 전후")에 직접 대응한다.
영향받는 명세·WP: F3 증거 game/tools/wp003_f3_evidence.gd(f3_ab.json: A/B setup·최초 관측·첫 볼리·종료 상태 + 개체별 장부),
  game/tools/f3_tracker.gd(개체 id별 관측 출처·볼리·처치/도달 시각), --capture=wp003_f3a|wp003_f3b 실제 화면.
결정 주체와 검증 증거: Claude Code.
대체하는 이전 결정: 없음.
```

```text
D-037 / 2026-09-15 / 채택:
문제와 선택: R-07 — HUD 분리. 좌상단 패널(폭 ≤ 816px, 광장(y≥320)·경복궁(x≥840) 밖): 런/방어 상태·양 거점 HP·회수 안내·조작.
  좌하단 패널(서대문 회랑 아래 y≥600, 남대문 회랑 왼쪽 x<880): 경로별 생존·밀도·화차·봉수망·인지 상세(D로 접기)와 알림.
  핵심 시설 HP 바·라벨은 마커 위쪽에 그려 바로 아래의 회수 후보 A/B와 겹치지 않는다. HUD 전체를 숨기는 방식은 쓰지 않았다.
근거와 고려한 대안: 1280×340 단일 패널이 내곽·핵심·미리보기를 덮었다(리뷰어 캡처 d/e). PanelContainer+autowrap Label로
  내용에 맞게 크기가 정해지므로 긴 줄이 패널 밖으로 나가지 않는다.
영향받는 명세·WP: AC-08 캡처 재생성.
결정 주체와 검증 증거: Claude Code. results/evidence/wp-003/captures/wp003_f2_d/e, wp003_f3*.
대체하는 이전 결정: 없음.
```

```text
D-038 / 2026-09-15 / 채택:
문제와 선택: R-05 — 성능 증거 집계·출처. (1) H1 배치 후 사격 = H1 자신의 배치 시점 누계 대비 델타(h1_shots_at_placement 기록).
  (2) 마지막 측정 프레임(recorder가 done으로 바뀌는 프레임)도 구간에 넣어 global 프레임 = 구간 합(segments_cover_all_frames)이며
  구간마다 frame_us_raw 인덱스 범위·t_measure 범위를 기록한다. (3) manifest는 live 데이터(존 표, 시작/종료 시설 목록, 시나리오별
  스크립트 명령·앵커)로 생성한다. (4) 실행 파일 SHA-256·크기를 게임이 자체 기록하고 perf_with_memory.ps1이 Get-FileHash로
  독립 계산해 대조한다(불일치 시 실패). 경로는 basename만 기록(개인 경로 금지). evidence SHA는 자기참조를 피해 결과 문서
  커밋에서 연결한다.
근거와 고려한 대안: 리뷰어 검산과 같은 방법(원시 배열 인덱스)으로 재계산 가능해야 한다.
영향받는 명세·WP: scripts/verify.*(6c 검사 추가).
결정 주체와 검증 증거: Claude Code.
대체하는 이전 결정: 없음.
```

## WP-004 플레이 흐름 기획 확정 (2026-09-16, GPT)

사용자의 플레이 기능 기획 반영 요청으로 [WP-004](../backlog/WP-004.md)를 READY로 작성한다. WP-003 DONE/PR #4 병합 `dd838ac`를 기준으로 한다. 새 메뉴 기능의 구현 AC는 아직 NOT RUN이다.

| ID | 결정 | 이유·영향 |
|---|---|---|
| D-039 | 일반 실행 TITLE, 전투/일시정지/설정/확인/결과 UI 상태 분리. Esc/P는 일시정지, Esc는 현재 메뉴 한 단계만 닫음 | 전투 런 상태와 메뉴 상태를 혼동하지 않음. 기존 일반 실행의 즉시 전투·Esc 종료를 대체. 자동 검증 모드는 기존 직접 실행 유지 |
| D-040 | 진행 중 재시작/시작 화면 복귀는 확인, 결과 뒤는 즉시. 같은 시드·초기 데이터 재시작. 메뉴 중 전투/배치 정지 및 입력 해제 후 새 전장 입력만 허용 | 잘못된 클릭으로 진행 소실·화차 배치 방지. R은 진행 중 확인창으로 변경. run_id와 held/pending 입력 무효화, 취소는 진입 상태 복원 |
| D-041 | 결과는 종료 시 실제 장부에서1회 생성, pause 제외 전투시간·웨이브·처치·도달·붕괴·회수/위치·핵심HP 표시. 설정은 창 모드만 우선 저장 | 재도전 선택에 필요한 정보 제공. 런 저장/이어하기 제외. 음량·흔들림/섬광은 실제 해당 기능 도입 시 설정 추가 |
| D-042 | 메뉴는 단순한 픽셀아트 표현·한글 가독성·내곽을 가리지 않는 배치. 1920×1080/1280×720 확인 | 사용자 그래픽 참조 방향 반영. 전장 에셋 전체 교체는 범위 밖. 타 게임 이미지/아이콘의 무단 에셋 전용 없이 자체 제작 |

WP-004는 HP360/60·18시설·10존·1140웨이브·회수 규칙을 변경하지 않는다. 자동 perf/capture는 메뉴와 저장 설정을 우회하고 기존 시험 부하·manifest 계약을 유지한다.

## WP-004 구현 기술 결정 (2026-09-16, Claude Code)

```text
D-043 / 2026-09-16 / 채택:
문제와 선택: UI 상태 기계의 위치. game/core/play_flow.gd(RefCounted, SceneTree 비의존)가 TITLE/PLAYING/PAUSED/SETTINGS/
  CONFIRM/RESULT와 복귀 대상(settings_return, confirm_return)·확인 종류를 갖고, 요청마다 "수락 시 scene이 수행할 행동"
  (new_run / to_title / quit / 없음)을 돌려준다. 거절된 요청과 전이는 순번과 함께 로그된다. scene(main.gd)은 행동만 수행한다.
근거와 고려한 대안: Godot 노드 상태 머신은 헤드리스 시험이 어렵다. 순수 클래스는 tests/test_play_flow.gd가 실제 Button 신호·
  합성 키 이벤트로 scene을 구동하면서도 전이 규칙을 단위로 검증할 수 있다. RunState(WON/LOST)와는 완전히 분리된다(D-039).
영향받는 명세·WP: WP-004 §1·§2.
결정 주체와 검증 증거: Claude Code. tests/test_play_flow.gd "PlayFlow unit", results/WP-004-RESULT.md.
대체하는 이전 결정: 없음.
```

```text
D-044 / 2026-09-16 / 채택:
문제와 선택: 입력 경계. 버튼/키 요청은 즉시 실행하지 않고 의도(intent) 큐에 넣어 다음 물리 프레임 시작에 순서대로 적용하며,
  큐에 넣을 때의 UI 상태와 적용 시점의 상태가 다르면 폐기(stale_intents 로그)한다. 전투 틱은 PLAYING에서만 돌고, 틱 뒤에
  런이 끝나면 RESULT로 바뀌므로 그 전에 들어온 pause/restart는 다음 프레임에 stale로 폐기된다(RESULT 우선). 같은 이유로
  더블클릭의 두 번째 클릭은 상태가 이미 바뀌어 폐기된다(전이 1회). 메뉴 진입 시 홀드 클릭·hover·미리보기를 비우고,
  메뉴가 열린 동안의 마우스는 Control(전체 화면 dim + 버튼)이 소비하며 키는 Esc 한 단계 닫기(및 RESULT의 R)만 처리한다.
  전장 배치는 press 이벤트에서만 일어나므로 메뉴를 닫은 클릭의 release는 아무것도 하지 않고 새 press부터 전장 입력이다.
  (2026-09-19 보완: "새 press부터"는 메뉴를 닫은 입력이 **해제된 뒤**의 새 press다. 해제 전 press는 D-047의 펜스가 거절한다.)
근거와 고려한 대안: 즉시 실행은 같은 프레임의 종료·중복 입력 순서를 보장하기 어렵다. 프레임 단위 큐는 결정적이고 시험 가능하다.
영향받는 명세·WP: WP-004 §2, AC-03/04/05.
결정 주체와 검증 증거: Claude Code. test_play_flow.gd AC-03/04/05, 캡처 로그의 state_before/after.
대체하는 이전 결정: R-03(D-035)의 초기화 규칙을 확장. 마지막 문장은 D-047이 보완.
```

```text
D-045 / 2026-09-16 / 채택:
문제와 선택: 결과 모델과 설정 저장. game/core/result_model.gd가 런 종료 프레임에 RunState/EnemySim/WaveDirector 장부에서
  한 번 사전을 만들고 flow.result에 복사해 동결한다(플레이 시간 = RunState.end_sim_time, 일시정지는 틱이 없으므로 구조적으로
  제외; 도달 수는 HP 역산이 아닌 arrivals 카운터). game/core/user_settings.gd는 ConfigFile [display] window_mode만 저장하며
  경로 주입(--settings, 캡처는 별도 임시 파일), 누락/파싱 오류/범위 밖은 기본값, 저장 실패는 결과를 돌려주고 메뉴에 알림.
  perf/capture/헤드리스 도구(bypass)는 settings를 아예 만들지 않는다.
근거와 고려한 대안: HUD 값 재사용은 렌더 타이밍에 흔들린다. 종료 장부 1회 확정이 계약(D-041)이다.
영향받는 명세·WP: WP-004 §4·§5·§6.
결정 주체와 검증 증거: Claude Code. test_play_flow.gd "ResultModel unit", "UserSettings unit", AC-06/07.
대체하는 이전 결정: 없음.
```

```text
D-047 / 2026-09-19 / 채택:
문제와 선택: 메뉴 닫기 입력의 해제 펜스(GPT 리뷰 R-01). 메뉴를 닫은 Esc/Enter/마우스 버튼/R을 떼기 전에 전장을 클릭하면
  회수 화차가 배치되었다(D-044는 메뉴에서 누른 클릭의 release만 다뤘다). main.gd가 `_input`(GUI보다 먼저, 모든 이벤트)과
  `_handle_key_event`(테스트·캡처의 합성 이벤트)에서 눌린 입력 장부 `_held`를 유지하고, 메뉴가 PLAYING으로 닫히는 프레임에
  그 시점 눌린 입력 집합을 펜스 `_fence`로 복사한다. 펜스가 비기 전(모두 release)까지 전장 명령(마우스 press, 1~4/T/C)을
  거절하고 `fenced_inputs`에 기록하며, 거절한 press는 홀드 재시도로 넘기지 않는다. release 뒤 새 press부터 배치한다.
  버튼은 release에 `pressed`를 내므로 마우스/Enter로 닫으면 닫히는 시점에 눌린 것이 없어 펜스가 비어 즉시 열리고,
  Esc·R처럼 press에 반응하는 키는 떼야 열린다. 창 포커스를 잃으면 release가 오지 않으므로 장부·펜스를 비운다.
  같은 회차에 `_physics_process`가 의도 큐 적용 전에 이미 끝난 런을 RESULT로 확정하도록 순서를 고정했다(리뷰의 종료 우선순위 관측).
근거와 고려한 대안: Input 싱글턴의 is_key_pressed는 헤드리스·합성 이벤트에서 상태가 없어 시험할 수 없다. "닫은 입력 하나"만
  추적하면 Enter로 닫는 경로(버튼이 release에 신호)를 다르게 다뤄야 하므로 "닫히는 순간 눌린 것 전부"를 펜스로 삼았다.
영향받는 명세·WP: WP-004 §2, AC-04/05.
결정 주체와 검증 증거: Claude Code. test_play_flow.gd "AC-05 R-01 release fence"(직접 핸들러 + Window.push_input 경로),
  "AC-05 run already LOST…", test_scene_modes.gd R-03(키 탭), 캡처 로그 `fence_probe` 2건/해상도.
대체하는 이전 결정: D-044의 마지막 문장을 보완.
```

```text
D-046 / 2026-09-16 / 채택:
문제와 선택: 메뉴 시각. game/scenes/menu_layer.gd — 둥근 모서리 없는 4px 목재색 테두리 패널, 낮은 채도의 기와색 배경,
  버튼은 돌색(기본)/주황(주 행동)/붉은색(런 폐기), 포커스 테두리 청록. 1920×1080 논리 좌표에 중앙 정렬하고 프로젝트의
  canvas_items/keep 스트레치로 1280×720에서도 같은 배치가 축소된다(캡처 wp004_ui_720). 외부 에셋·아이콘 없음. 메뉴가 닫히면
  기존 HUD 배치가 그대로 보인다(HUD는 TITLE에서만 숨김). 우상단에 일시정지 버튼 하나를 추가했다.
근거와 고려한 대안: 픽셀아트 전장 교체는 범위 밖(D-042). 시스템 폰트 폴백으로 한글 가독성을 우선했다.
영향받는 명세·WP: WP-004 §5, AC-07.
결정 주체와 검증 증거: Claude Code. results/evidence/wp-004/captures/*.png.
대체하는 이전 결정: 없음.
```

## D-048 / 2026-09-20 / 채택: 그래픽 샘플부터 단계적 적용

- 근거: 사용자의 그래픽 제작·적용 계획 및 역할 계획 후 “다음 진행” 요청. WP-004 DONE/PASS, main `51d89ed` 확인.
- 결정: WP-005는 광화문 앞 샘플을 제작·적용하는 READY, WP-006 전체 전장 확장과 WP-007 연출·UI 마감은 DRAFT로 둔다. 각 작업의 담당·인수 산출물·검증 기준을 문서화한다.
- 역할: GPT는 명세·제작 지시·아트 검수, Claude는 적용·자동 보정·테스트·빌드, 이미지 도구는 원본 시안 제작을 담당한다. 별도 픽셀 편집 인력은 아직 배정되지 않았으며 자동 처리 불가 항목을 완료로 간주하지 않는다.
- 기준: ART_GUIDE의 탑다운 픽셀아트, 20px 셀·40px 시설 점유 보존. 렌더 교체로 전투 규칙을 바꾸지 않는다. 첫 규격/수량은 샘플 제작 기준이며 조정은 근거를 남긴다.
- 확인: 사용자의 실제 샘플 스타일 확인은 WP-005 AC-08이며 전체 확장 선행 조건이다. READY 자체는 에셋/스타일 승인·구현 완료가 아니다. 일상적인 파일 작업에는 반복 승인을 요구하지 않는다.
- 검증: greybox/sample 동일 전투 상태, 기존 회귀, 두 해상도, 1,000체 release 성능과 출처/권리·에셋 추적. 제작 현황은 ASSET_MANIFEST의 실제 상태로 기록한다.
- 변경 범위: D-042의 전장 아트 후속 작업을 WP-005로 구체화한다. WP-001~004의 승인된 전투·입력 기준은 대체하지 않는다.

## D-049 / 2026-09-20 / 채택: 아트 적용 파이프라인 — 렌더링 선택은 시뮬레이션 밖

- 근거: WP-005 §전투·입력·성능 보존(`--art=greybox|sample`은 렌더링만 바꾼다), ART_GUIDE 크기·좌표·출력, D-048.
- 결정: `game/scenes/art_set.gd`가 `assets/art/wp005/<subdir>/<asset_id>_<state>_v01.png`(N프레임 = 가로 스트립)를 읽어 프레임을 자르고 적 아틀라스(4열×3행, 2px 여백)를 합성한다. 없는·규격 불일치 파일은 누락 목록에 남고 그 요소만 회색상자로 그린다(부분 적용 허용, 완성 판정은 manifest). 기준점은 시설·거점 `(폭/2, 높이-20)`, 적 `(폭/2, 높이)`, 효과·타일 중앙; `pivots.json`로 파일별 대체. 계약과 도구 점검 결과는 `docs/art/source/WP005_REQUEST.md`.
- 렌더: 지형은 `terrain_layer.gd`의 순수 타일 계획(`sample_plan`: 바닥 4종 해시, 벽 접면 가장자리 0-3·안쪽 모서리 4-7, 거리 접면 담장/뒤쪽 지붕 모듈 반복, 광화문 문 1회)을 샘플 구역 x30..65/y6..30에만 그린다. 시설·거점·표시는 `overlay_layer.gd`가 core 상태(활성/그룹/muzzle/HP/붕괴/회수)로 프레임을 고르고, 적은 기존 MultiMesh 1회 업로드에 custom data(아틀라스 프레임 = 흐름장 방향 + 2프레임 교대)를 더해 정점 셰이더가 UV를 고른다(개체별 노드 없음). 회색상자 경로는 12 float/개체 그대로.
- 효과: `fx_layer.gd`는 실제 이벤트에만 1:1로 생성한다 — 화차 render_queue 1건 = fire 1 + impact 1, EnemySim render_events(처치/피해 위치 장부, 256건 cap) = despawn/hit, RunState.collapse_count 증가 = collapse 1. 시뮬레이션 시간으로만 진행하므로 메뉴에서 멈추고, 새 런은 `_reset_input_state`가 전부 비운다. 회색상자에서는 fx 레이어를 만들지 않는다.
- 경계: `--art`·`--art-dir`·`--labels`는 scene 인자다. Config·시드·자동 모드에 닿지 않으며 greybox/sample의 `full_state`는 체크포인트마다 같아야 한다(테스트·캡처 로그 `state_log`로 검사). core 변경은 render 장부 2곳(EnemySim.render_events, Hwacha.render_queue의 화차 중심)뿐이고 어느 것도 시뮬레이션이 읽지 않는다.
- 검증: `tests/test_art_sample.gd`(로더/아틀라스/타일 계획/적 프레임/greybox==sample 6체크포인트/FX 계약/배선), `--capture=wp005_<art>[_720]`(state_log·fx·sprites·tiles 기록), `verify.ps1 -Wp005`(fixture 생성, 4캡처, 모드별 collapse 성능). 개발용 fixture(`game/tools/wp005_dev_fixture.gd`, `user://`)는 에셋이 아니다.
- 대체하는 이전 결정: 없음(D-044·D-047의 입력 규칙, D-027 성능 계약 유지).

## D-050 / 2026-09-20 / 채택: 생성 시안의 자동 보정 적용과 단일 로더 통합

- 근거: GPT 파일럿 브랜치 `wp/005-facility-art-pilot`(PR #10, `26b9d70`/`17f8c24`)가 시설 4종 생성 원본(1254×1254 RGBA, PROMPTS.md·source_inspection.json·style_sheet)을 제공했다. ART_GUIDE 제작·역할: Claude는 "실행 가능한 자동 보정·파일 정리", 큰 시안 축소만으로 완성을 판정하지 않는다.
- 결정: 원본은 `docs/art/source/wp005/`에 보존하고, `scripts/art_convert_wp005.py`가 알파 경계 크롭 → 긴 변 40px BOX 축소 → 24색 팔레트 → 이진 알파 → 바닥 중심 접지로 `assets/art/wp005/facilities/<id>_<state>_v01.png`(40×40)을 만든다. 비활성/단절 상태는 원본이 없어 채도 0.25·밝기 0.72로 **파생**(manifest에 `derived_off` 표기, 전용 프레임이 오면 교체). 발사·점등 프레임은 미제작 → 누락 유지(로더가 회색상자/정적 프레임으로 대체). manifest 상태는 `PILOT_DOWNSCALED_STATIC`이며 AC-01 완료가 아니다.
- 통합: 파일럿의 `facility_art.gd`(런타임에서 1254px 텍스처를 36px로 축소, 비활성은 틴트+X)와 1254px 런타임 사본은 제거하고 D-049 로더 한 경로만 둔다(렌더 경로 2개 유지 금지, 런타임에 필요한 파일만 포함). 파일럿 증거(`results/evidence/wp-005/{greybox,sample}_*.png`, `render_checks.json`, `capture_pilot.gd`, 파일럿 test_report)는 기록으로 보존한다.
- 기본 모드: `--art` 미지정 시 일반 실행은 `sample`(검수 파일 표시, 없는 요소는 회색상자), `--perf`/`--capture`는 `greybox`(파일럿 브랜치의 선택을 수용; 승인된 WP-001~004 증거 절차 불변). 테스트 스위트는 모드를 명시한다.
- 검증: `tests/test_art_sample.gd` "Reviewed assets"(기본 디렉터리 로드·40×40·pivot·접지·누락 보고·기본 모드·상태 동일), `--capture=wp005_assets[_720]`(기본 디렉터리, greybox 대비 상태 동일·로드 수 = 배포 파일 수·시설 스프라이트 그려짐), `verify.ps1 -Wp005` 4e 확장.
- 대체하는 이전 결정: 없음(D-049 보완).

## D-051 / 2026-09-20 / 채택: PR #11 부분 통합 수용과 검수 REVISE 반영

- 근거: GPT의 `wp/005-art-integration`(PR #11, `f0c625a`/`c5ea366`)이 회차 1 파이프라인 위에 리소스 13개 상태 파일(시설 기본 4, 바닥 4프레임, 지붕·담장, 적 6상태 12프레임)과 배포본 로딩 수정, 시각 검수(REVISE: AC-01 22개 누락, AC-03 비활성 회색 도형, 바닥 체크무늬, 라벨 없는 식별·1,000체·동일 release 성능·사용자 확인 필요)를 제출했다.
- 결정: 통합 브랜치를 `wp/005-art-sample`에 병합(`f565a52`). 원본 슬라이싱·정규화는 통합 브랜치의 `game/tools/import_wp005_sources.gd`(nearest, 시설 36px 맞춤·바닥 2px 여백)를 기준으로 하고, 회차 2의 BOX+팔레트 변환은 폐기한다. `scripts/art_convert_wp005.py`는 통합본 idle/active 파일에서 inactive/disconnected를 파생하는 역할만 남긴다(`derived_off`, 전용 프레임 도착 시 교체). 접지 검사는 캔버스 바닥 2px 이내로 둔다.
- 배포본: `art_set.gd`의 ResourceLoader 폴백(export가 PNG를 가져온 텍스처로 바꿔 FileAccess 경로가 사라지는 문제)과 `export_presets.cfg` include_filter(`assets/art/wp005/**/*.png, *.json`)를 유지한다. 원본 SHA는 동봉 `integration_manifest.json`으로 추적한다.
- 검수 반영: 바닥 타일 계획을 기본 타일 약 69% + 변형 희소 배치로 바꿔 체크무늬를 완화한다(`terrain_layer.gd`, 4종 모두 사용). 캡처 `wp005_dense[_720]`(D-027 벤치마크 부하 1,000체, 라벨 on/off, 붕괴·재배치)로 AC-03 라벨 없는 식별과 AC-06 1,000체 화면을 낸다. `verify.ps1 -Wp005` 6d는 sample 성능을 배포 리소스 디렉터리로 측정한다(fixture는 파이프라인 캡처 1종에만).
- 남는 것: 발사·점등·거점·문·가장자리 타일·효과·표시 제작(GPT), 사용자 스타일 확인(AC-08). 건물 실루엣은 WP-006 범위 여부를 GPT가 정한다.
- 대체하는 이전 결정: D-050의 BOX+팔레트 변환 경로(폐기). 나머지 D-049/050 유지.

### D-051 보완 / 2026-09-20 / PR #12 통합

상위 `274ad7a`의 기본 sample 실행·희소 바닥·1000체 캡처·추가 테스트를 유지한다. 비활성4종은 D-051의 임시 파생본 대신 전용 생성 원본(`REVISION_PROMPTS.md`)을 가져온 그림으로 교체한다. 기존 파생 도구는 전용 원본이 있으면 파일/manifest 해시를 확인하고 보존한다. R-01은 모든 적 상태를 포함한 공통 배율로 잘림을 방지한다. 양쪽 결과/증거는 보존하며 합친 버전의 검증은 `revision1-merged/`에 새로 기록한다. core 규칙은 변경하지 않는다.

## D-052 / 2026-09-20 / 채택: 표현 보정 — 적 테두리, 절차적 표시·가장자리, 근접 증거

- 근거: 인계문 `docs/CLAUDE_HANDOFF.md`(어두운 길 위 적 대비, 작은 화면의 소등 식별, 지형 경계·점유 정합, 근접/점유 비교·두 해상도 메뉴·1,000체 검수) 및 GPT 검수. 이미지 생성 도구가 없으므로 리소스 픽셀은 바꾸지 않고 렌더링에서 보정한다.
- 적 테두리: 적 아틀라스 정점/프래그먼트 셰이더가 투명 텍셀 중 불투명 이웃이 있는 곳을 밝은 림(`outline_color`)으로 칠한다(`--art-outline=on|off`, 기본 on, 캡처 스텝 `outline`). 아틀라스 여백이 투명이라 프레임 간 번짐이 없다. 리소스 파일·아틀라스 픽셀 불변, 성능은 배포본으로 재측정.
- 절차적 표시: `interaction_marks` 5종은 WP가 허용한 절차적 도형으로 확정(manifest `PROCEDURAL`; PNG가 오면 로더의 선택 항목으로 대체 가능). 로더는 선택 항목 누락을 `optional_missing`으로 따로 보고한다. 비활성·단절 시설에는 회색 원+사선의 소등 표시를 추가해 색 변화에만 의존하지 않는다(ART_GUIDE). 오버레이는 그린 표시를 `marks_drawn` 장부로 남기고 캡처 로그가 이를 기록한다.
- 가장자리: `terrain_sample/edge` PNG가 올 때까지 타일 계획의 edge 항목을 절차적 벽 접면 선(2px)·모서리 점(3px)으로 그린다(`sample_tiles_drawn.edge_procedural`).
- 근접·정합 증거: 캡처 스텝 `zoom`(Camera2D, 월드 레이어만)과 `footprints`(시설 40×40 점유·샘플 구역 20px 격자)로 `wp005_closeup` 시나리오를 추가한다. 메뉴·문자·배치 표시는 `wp004_ui`/`_720`을 `--art=sample`로 실행해 확인한다. 카메라·오버레이는 전투 상태에 닿지 않는다(테스트).
- 대체하는 이전 결정: 없음(D-049~051 보완).

