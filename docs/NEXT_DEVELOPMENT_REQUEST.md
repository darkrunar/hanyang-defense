# 다음 개발 요청 — WP-008 건설·물자

한양 디펜스의 구현 담당으로 WP-008을 진행한다. 사용자 지정 로컬 저장소를 확인하고 기존 변경을 보존한다. 최신 계획 커밋을 포함한 기준에서 `wp/008-build-economy` 브랜치를 만들고 착수 SHA를 기록한다. main에 직접 작업/병합하지 않는다.

## 먼저 읽기

CLAUDE.md → docs/DECISIONS.md D-054 → docs/GAMEPLAY_EXPANSION_PLAN.md → backlog/WP-008.md → docs/SYSTEM_SPEC.md → docs/CORE_LOOP.md → results/RESULT_TEMPLATE.md.

## 이번 목표

새 `--play-mode=build`에서 최소 4시설·물자240으로 시작해 준비 배치 → 방어 시작 → 처치/웨이브 물자 → 전투 중 건설 → 붕괴 후 무료 회수와 내곽 증설 → 승패·재시작을 구현한다. 수치·거절·정산·입력·회수 규칙은 WP-008을 그대로 따른다.

WP-004 DONE을 기능 기반으로 사용한다. D-054에 따라 WP-005는 독립 그래픽 트랙이므로 미완을 이유로 WP-008을 막거나 WP-005를 임의 DONE 처리하지 않는다. 기존 기본 실행과 WP-001~005 fixture/증거를 보존한다. 성밖/성문/장승 파괴(WP-009/010), 전체 맵 아트, 재화 확장·스킬·보스는 이번에 구현하지 않는다.

## 진행

1. Git 상태·환경·기준 문서 확인 후 기술 설계, 명령/경제 장부 구조, 테스트 fixture와 성능 구성 계획을 기록하고 계속 진행한다.
2. 비용 검사·유효성 검사·생성·차감을 한 명령의 원자적 결과로 처리한다. 거절/중복 입력/이전 런 이벤트가 돈을 쓰거나 늘리지 않게 한다.
3. 유료 건설과 무료 회수를 명확히 구분하고 메뉴 닫기 입력 해제 계약을 유지한다. 준비 상태·전투·메뉴·결과·재시작 전체 흐름을 구현한다.
4. AC-01~09 및 기존 전체 회귀를 실행한다. 실제 두 해상도 release 화면과 원시 성능 JSON을 저장하고 발견한 구현 오류를 수정한다. 밸런스 실패는 기준 결과와 변경 제안으로 보고하고 기준을 몰래 낮추지 않는다.
5. results/WP-008-RESULT.md와 results/evidence/wp-008/에 기준/구현 SHA, 변경 파일, 재현 절차, AC별 PASS/FAIL/NOT RUN, 화면·경제 장부·성능·알려진 문제를 기록한다.
6. 코드와 결과를 커밋/푸시하고 Draft PR을 만든다. base가 아직 main에 병합되지 않은 WP-005 브랜치라면 해당 브랜치를 base로 한 stacked Draft PR로 WP-008 diff를 분리한다. 의존 브랜치와 착수 SHA를 PR에 명시한다.

구현·필수 검증 제출 후 REVIEW / GPT Review PENDING. 필수 항목 미완이면 IN_PROGRESS와 미완 이유를 기록. 모든 필수 AC 및 GPT PASS 전 DONE 금지. 완료 보고는 구현 내용·테스트·남은 문제·실행 방법·결과 문서·PR 링크 순으로 제공한다.
